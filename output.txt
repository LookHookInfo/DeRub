Я понял. Кнопка "Add Liquidity" в компоненте `VaultManager` уже работает именно так. Она становится активной только тогда, когда в хранилище есть и DRUB, и HASH (то есть, `drubBalance` и `hashBalance` больше нуля) и при этом нет других ожидающих транзакций. Это гарантирует, что кнопка активна только "если есть возможность добавить" ликвидность.

Таким образом, желаемая вами логика уже реализована.